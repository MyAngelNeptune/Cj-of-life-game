package screens;

import com.death.DeathGame;
import Character.PlayableCharacter;
import Weapon.Weapon;

public class LevelOne extends Level {
    public LevelOne(DeathGame game, String bg, PlayableCharacter player, Weapon weapon, String mapFile) {
        super(game, bg, player, weapon, mapFile);
    }
}
